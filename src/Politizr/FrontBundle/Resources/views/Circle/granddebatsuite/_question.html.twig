{% if not circle.readOnly %}
    <div class="grpIHaveAQuestion">
        <h2>Question, avis, remarque ou opinion...</h2>
        <p>Exprimez vos propositions !</p>
        <a href="{{ path('DebateDraftNew', { topic: topics[0].uuid }) }}">Je m'exprime!</a><a href="{{ path('TopicDetail', { circleSlug: circle.slug, slug: topics[0].slug }) }}" class="grpAllQuestions">Voir les propositions déjà exprimées</a>
    </div>
{% endif %}