<div id="logo"><a href="{{ path('Homepage'~profileSuffix()) }}"><i class="icon-pltzr-logo"></i><span>politizr</span></a></div>
<div id="headerMenu">
    <a href="{{ path('CircleDetail', { 'slug': circle.slug }) }}">{{ circle.title }}</a>
</div>
{% if circle.pCircleTypeId != constant('Politizr\\Constant\\CircleConstants::CIRCLE_TYPE_FREE') %}
    <div id="searchInput">
        <i class="icon-search"></i><input id="searchInputAlgolia" type="text" placeholder="Rechercher" circleUuid="{{ circle.uuid }}">
    </div>
{% endif %}
